const { resolve } = require('path');

module.exports = {
  // Caminho para o arquivo de configuração do banco de dados
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  // Caminho para a pasta de models
  'models-path': resolve(__dirname, 'src', 'models'),
  // Caminho para a pasta de migrações do banco de dados
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
  // Caminho para a pasta de seeds do banco de dados
  'seeders-path': resolve(__dirname, 'src', 'database', 'seed')
};
